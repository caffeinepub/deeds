# Specification

## Summary
**Goal:** Revert the logo images in the Header and LoginPrompt/hero components back to the previously saved Deeds logo asset.

**Planned changes:**
- Update the Header component logo `img src` to `/assets/generated/deeds-logo.png`
- Update the main/hero section (Gate or LoginPrompt component) logo `img src` to `/assets/generated/deeds-logo.png`

**User-visible outcome:** The original saved Deeds logo is displayed in both the top navigation header and the hero/main landing area of the page.
